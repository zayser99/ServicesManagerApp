import 'package:flutter/material.dart';

class PRueba extends StatelessWidget {
  const PRueba({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Text('Haaaaaa'),
    );
  }
}
